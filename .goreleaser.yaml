version: 2

builds:
  - skip: true

archives:
  - formats: [tar.gz]

checksum:
  name_template: checksums.txt

changelog:
  disable: true

release:
  github:
    owner: yeisme
    name: agent-skills-runtime
  draft: true

